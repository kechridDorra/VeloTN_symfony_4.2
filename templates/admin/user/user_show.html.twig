
{% extends 'admin/baseAdmin.html.twig' %}

{% block body %}
  <center> <h1>Detail Utilisateur</h1></center>
<form>
    <center>
        <div class="form-outline mb-4">
        <div class="col-md-6">
        <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user.id }}</td>
        </tr>
        <tr>
            <th>Username</th>
            <td>{{ user.username }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.prenom }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user.password }}</td>
        </tr>
        <tr>
            <th>Date_nais</th>
            <td>{{ user.dateNais ? user.dateNais|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Tel</th>
            <td>{{ user.tel }}</td>
        </tr>
        </tbody>
    </table>
    <button class="btn btn-warning" href="{{ path('list_user_admin') }}"> Retour à la liste &nbsp</a>
    </button>
    <button class="btn btn-secondary"href="{{ path('admin_user_edit', {'id': user.id}) }}"> Modifier </a>
    </button></div><br>
    {{ include('admin/user/_delete_form.html.twig') }}
</div>
    </center></div></center>
</form>
    <br>

{% endblock %}


